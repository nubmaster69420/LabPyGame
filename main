import pygame as pg
import sys
import random as rd
 
running = True
while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False                
